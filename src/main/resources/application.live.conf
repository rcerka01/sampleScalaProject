include "application.conf"

kamon {
  statsd {
    simple-metric-key-generator {
      hostname-override = "live"
    }
  }
}

client {
  tupac {
    host = "",
  }
}

database {
  properties = {
    serverName = ""
    databaseName = "programmes"
    portNumber = 5432
    user = "developer"
  }
}

aws {
  sns {
    tupac-actions-topic = ""
  }
}